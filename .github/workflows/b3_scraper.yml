name: B3 Intraday Data Scraper

on:
  # Executa automaticamente em horários específicos (UTC-3 = horário de Brasília)
  schedule:
    # Dias úteis (segunda a sexta) às 21:00 BRT (00:00 UTC do dia seguinte)
    - cron: '0 0 * * 1-5'
    # Também às 23:30 BRT para pegar fechamento do after-market
    - cron: '30 2 * * 1-5'
  
  # Permite execução manual via botão "Run workflow"
  workflow_dispatch:
    inputs:
      interval:
        description: 'Timeframe para coleta'
        required: true
        default: '5m'
        type: choice
        options:
          - '1m'
          - '5m'
          - '15m'
          - '30m'
          - '1h'
      tickers:
        description: 'Tickers específicos (separados por vírgula, deixe vazio para padrão)'
        required: false
        default: ''

jobs:
  scrape-b3:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      # 1. Checkout do repositório
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Permite commit dos dados depois
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      # 3. Instala dependências
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r scraper/requirements.txt
      
      # 4. Executa o scraper
      - name: Run B3 Scraper
        env:
          # Configurações via variáveis de ambiente
          B3_TICKERS: ${{ github.event.inputs.tickers || 'ABCB4,ABEV3,AGRO3,ALOS3,ALPA4,ANIM3,ASAI3,AZZA3,B3SA3,BBAS3,BBDC3,BBDC4,BBSE3,BEEF3,BPAC11,BRAP4,BRAV3,BRKM5,BRSR6,CAML3,CASH3,CBAV3,CEAB3,CMIG4,CMIN3,COGN3,CPFE3,CPLE3,CSAN3,CSMG3,CSNA3,CURY3,CVCB3,CYRE3,DIRR3,DXCO3,ECOR3,EMBR3,ENEV3,ENGI11,EQTL3,EZTC3,FLRY3,GGBR4,GGPS3,GMAT3,GOAU4,GRND3,GUAR3,RIAA3,HAPV3,HYPE3,ITSA4,ITUB3,ITUB4,JHSF3,KEPL3,KLBN11,LJQQ3,LREN3,LWSA3,MDIA3,MGLU3,MOTV3,MOVI3,MRFG3,MRVE3,NEOE3,NTCO3,ONCO3,PCAR3,PETR3,PETR4,PGMN3,PLPL3,POMO4,POSI3,PRIO3,PSSA3,QUAL3,RADL3,RAIL3,RAIZ4,RAPT4,RDOR3,RECV3,RENT3,SANB11,SAPR11,SBSP3,SEER3,SIMH3,SLCE3,SMFT3,SMTO3,SUZB3,TAEE11,TIMS3,TOTS3,UGPA3,USIM5,VALE3,VAMO3,VBBR3,VIVA3,VIVT3, VULC3,WEGE3,YDUQ3' }}
        run: |
          python scraper/b3_scraper.py \
            --interval ${{ github.event.inputs.interval || '5m' }} \
            --data-dir data/intraday
      
      # 5. Verifica se há dados novos para commit
      - name: Check for changes
        id: verify-changed-files
        run: |
          if [ -n "$(git status --porcelain data/)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi
      
      # 6. Commit e push dos dados (se houver mudanças)
      - name: Commit data changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Adiciona apenas arquivos de dados
          git add data/
          
          # Commit com timestamp
          git commit -m "📊 Auto-update: Dados intraday B3 $(date +'%Y-%m-%d %H:%M')"
          
          # Push
          git push
      
      # 7. Upload artifacts (backup dos dados)
      - name: Upload data artifacts
        uses: actions/upload-artifact@v4
        with:
          name: b3-intraday-data-${{ github.run_id }}
          path: data/intraday/
          retention-days: 30
      
      # 8. Notificação de sucesso/falha (opcional)
      - name: Create issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.name,
              title: `🚨 Falha na coleta B3 - ${new Date().toISOString().split('T')[0]}`,
              body: `Workflow falhou na execução: ${context.runId}\nVerifique os logs em: ${context.serverUrl}/${context.repo.owner}/${context.repo.name}/actions/runs/${context.runId}`
            })
